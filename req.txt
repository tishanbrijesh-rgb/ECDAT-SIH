# ECDAT-SIH Python runtime and API test dependencies.
# Install from the repository root: python -m pip install -r req.txt
# The dashboard is installed independently with: npm ci --prefix dashboard
-r backend/requirements-dev.txt
